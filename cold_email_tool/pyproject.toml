[project]
name = "cold-email-tool"
version = "0.1.0"
description = "Cold email personalization tool — research, personalize, and draft high-signal outreach."
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.39.0",
]

[project.scripts]
cold-email = "cold_email_tool.cli:main"
